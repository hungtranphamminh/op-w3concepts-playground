"use client";
import { useState, useEffect, useCallback } from "react";

export default function TransferButton() {
  return (
    <div>
      <button className="px-[12px] py-[6px] rounded-lg border border-black">
        Transfer
      </button>
    </div>
  );
}
